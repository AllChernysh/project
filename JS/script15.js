"use strict";

console.log('arr' + " -object" );
console.log(4 + +"5");

let incr = 10,
    decr = 10;

// ++incr;
// --decr;


console.log(++incr);
console.log(--decr);


console.log(5%2); 

console.log(2*4 == 8); // acc true/falls
console.log(2 + 2 * 2 != 8); // acc type number/string

// && // and
// || // or

const isChecked = true;
const isClose = false;

console.log(isChecked && isClose);
console.log(isChecked || isClose);

// ! --- отрецание (не равен)
